package secure_shop.backend.enums;

public enum PaymentProvider {
    MOMO,  // Giữ lại để tránh lỗi mapping Hibernate với DB cũ
    VNPAY, // Giữ lại để tránh lỗi mapping Hibernate với DB cũ
    NONE
}
